import React from 'react';
import ReactDOM from 'react-dom/client';
import PopupPage from '../../src/pages/popup/PopupPage';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PopupPage />
  </React.StrictMode>,
);
